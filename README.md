# httpx_bash
mini scripts on bash to use httpx toolkit

To use htis script Install httpx and chmod all bash scripts:

    sudo apt install golang && go install -v github.com/projectdiscovery/httpx/cmd/httpx@latest && httpx -h && cd httpx_bash && chmod +x *
